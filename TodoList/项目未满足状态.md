# 项目未满足状态

更新时间：2026-09-20  
项目路径：`/Users/mengxiangchen/Code/pyAgent/EquityWorkupAgent`  
最近一次完整运行：`outputs/20260919_234638_517469_000938`（公司 `000938` 紫光股份）

本文件只记录**当前尚未满足的状态**。项目"是什么"见根目录 `README.md`。

## 1. 结论

- 端到端链路可运行：`self_check` 与 `finalize` 均 `completed`，`result.docx` 可重开并渲染为 26 页。
- 字段结果：**59 成功 / 36 失败 / 0 冲突**（配置字段总数 95）。
- 严格验收 `scripts/audit_full_acceptance.py <run_dir> --render`：**status = FAIL**。
- 唯一阻塞项：`visual_inspection`（需要人工逐页检查 26 页 PNG 并填写清单）。
- 其余"未满足"是**字段级**的：36 个字段仍为具体失败，尚未成功产出。

## 2. 验收现状

严格验收共 21 项检查，19 项通过、2 项未通过：

| 检查项 | 结果 | 说明 |
|---|---|---|
| `extracted_data_has_no_node_errors` | FAIL | 48 个 node_error（信息项，不阻塞） |
| `visual_inspection` | FAIL | 唯一阻塞项，需人工完成 |

已通过的关键检查：95 字段全部可解释（`every_configured_field_accounted_for`）、每个失败都有原因（`every_failure_has_reason`）、0 冲突、每个成功字段有 value 与 source_url、成功值与 DOCX 映射一致（59 个）、DOCX 可重开、渲染 26 页 returncode 0。

### 完成严格验收所需的最后一步

1. 逐页查看 `outputs/20260919_234638_517469_000938/rendered/page-1.png` … `page-26.png`。
2. 填写 `outputs/20260919_234638_517469_000938/rendered/visual_inspection.template.json`（每页 `status=pass` + `notes`）。
3. 重新运行：`.venv/bin/python scripts/audit_full_acceptance.py outputs/20260919_234638_517469_000938 --render --inspection-manifest outputs/20260919_234638_517469_000938/rendered/visual_inspection.template.json`

## 3. 未成功的 36 个字段

按根因分组：

| 根因 | 数量 | 字段范围 |
|---|---:|---|
| part_06 数值字符串被预检查丢弃 | 18 | `current_ratio_*`、`quick_ratio_*`、`capex_*`、`interest_coverage_*`、`debt_to_asset_*`、`ocf_positive_*`、`cash_flow_gt_noi_*`、`sustained_positive_ocf`、`net_income_exceeds_ocf` |
| part_11 证据锚定失败 | 14 | `auditor_profile`、`audit_opinion`、`qualified_opinion_details`、`auditor_change_*`、`financial_restatement_*`、`board_officer_material_change`、`business_operation_material_change`、`top3_shareholder_change`、`nonfinancial_change_details`、`litigation_status`、`litigation_regulatory_details`、`negative_news_summary` |
| part_01 证据锚定失败 | 3 | `soe_classification`、`listed_outside_directorships`、`major_ma_past_12m` |
| 无验证候选值 | 1 | `us_revenue_ratio` |

### 3.1 part_06：数值字符串被预检查丢弃（18 个，占比最大）

- 现象：`annual_inputs[0].current_assets.value must be a JSON number, not a numeric string`。
- 性质：模型返回的数值以字符串形式给出，被 part_06 预检查**先丢弃**，未进入强转路径。
- 影响：整个 part_06 组（18 个字段）全部失败；part_06 节点状态为 `partial`。
- 说明：其余 Part（05/07/08/11Legal/11Litigation/11Regulatory）已通过显式 allowlist 强转修复，part_06 的 annual 指标未纳入同一处理，**这是当前最大的单点失败源**。
- 决策记录：业务方已裁定本项**暂不修改，直接收尾**；若修复，预期成功率可提升到约 77/18。

### 3.2 part_11：证据锚定失败（14 个）

- 现象：`evidence fact could not be anchored in supplied pages: https://disc.static.szse.cn/...`。
- 性质：模型给出的证据片段无法在 PDF 页面文本中定位，字段级失败（失败隔离已生效，不再清空整组）。
- 影响：审计、重述、重大变化、诉讼监管、负面新闻等字段；part_11 节点状态为 `partial`。
- 待定位：是 PDF 表格换行/单元格拆分导致锚定失败，还是模型改写原文；需要逐字段核对。

### 3.3 part_01：证据锚定失败（3 个）

- `soe_classification`、`listed_outside_directorships`、`major_ma_past_12m` 同样因证据无法锚定而失败；part_01 节点状态为 `partial`。

### 3.4 us_revenue_ratio：无验证候选值（1 个）

- 现象：`No verified candidate returned for us_revenue_ratio; configured sources attempted: derived.`
- 性质：派生来源未产出可验证候选值。

## 4. 节点状态

| 节点 | 状态 | 说明 |
|---|---|---|
| `shared_disclosures` | partial | 部分公告/报告未成功解析 |
| `part_01` | partial | 3 个字段证据锚定失败 |
| `part_06` | partial | 18 个字段数值字符串被丢弃 |
| `part_11` | partial | 14 个字段证据锚定失败 |
| 其余节点 | completed | part_02/03/04/05/07/08/09/10 等 |

`node_errors` 共 48 条：part_06 = 36、part_11 = 9、part_01 = 3。验收脚本已将 `extracted_data_has_no_node_errors` 列为**信息项**（`INFORMATIONAL_CHECKS`），不阻塞 PASS。

## 5. 已知技术债与搁置项

1. **RC5 allowlist 复发隐患**：`NUMERIC_COERCION_MODELS` 是显式 allowlist，新增模型若返回数值字符串仍会失败，需要更通用的处理或纳入测试约束。
2. **`json.loads` → float 精度**：金额超过 15 位有效数字时可能精度损失。
3. **part_04 级联观察**：`part_04` 状态为 `completed`，但 `annual_records` 为 0、`raw_result` keys 为空，可能级联影响 part_06/part_07 的年度输入。
4. **扫描件 PDF**：4 个 PDF 无文本层（扫描件），OCR 明确超出当前范围，会削弱部分字段证据。
5. **负面新闻发现链路**：需区分"确实无负面新闻"与"搜索发现链路不可用"；当前 `negative_news_summary` 失败。
6. **外部源稳定性**：巨潮、雪球、东方财富、搜索源在不同网络环境下可能失败（历史出现过巨潮 403、雪球 400、摘要模型 401）。
7. **未处理的评审 minor**：最终全分支评审中的 minor 4–12 未处理。

## 6. 不在当前范围（无需满足）

- 运行时解析任意 Word 模板并自动生成字段定义。
- 非 A 股公司或存在歧义的公司名。
- 全市场自动同行发现（当前为固定三家同行）。
- 未经业务批准更换行情数据源。
- 手工填充 DOCX 绕过证据链。

## 7. 验收基线

| 指标 | 值 |
|---|---:|
| 配置字段总数 | 95 |
| 成功字段 | 59 |
| 失败字段 | 36 |
| 冲突 | 0 |
| node_error | 48 |
| 渲染页数 | 26 |
| 单元测试 | 254 passed |
| 严格验收 | FAIL（仅 `visual_inspection`） |
